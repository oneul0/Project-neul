apply plugin: 'java'

dependencies {
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.15.3'
    implementation 'com.fasterxml.jackson.datatype:jackson-datatype-jsr310:2.15.3'
}

// Ensure it's treated as a plain library JAR
jar {
    archiveClassifier = ''
}
