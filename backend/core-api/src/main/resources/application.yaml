spring:
  application:
    name: core-api
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:../.env[.properties]
      - optional:file:../../.env[.properties]

  r2dbc:
    url: r2dbc:postgresql://${GAK_POSTGRES_HOST:127.0.0.1}:${GAK_POSTGRES_HOST_PORT:5432}/${GAK_POSTGRES_DB:gak_db}
  flyway:
    enabled: true
    url: jdbc:postgresql://${GAK_POSTGRES_HOST:127.0.0.1}:${GAK_POSTGRES_HOST_PORT:5432}/${GAK_POSTGRES_DB:gak_db}
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration
  sql:
    init:
      mode: never

  data:
    redis:
      host: 127.0.0.1
      port: 6379

  kafka:
    bootstrap-servers: 127.0.0.1:9092
    consumer:
      auto-offset-reset: earliest

server:
  port: 8083
  error:
    include-message: always
    include-stacktrace: never

app:
  chzzk:
    live-status-base-url: https://api.chzzk.naver.com/service/v2/channels
  collector:
    base-url: http://localhost:8081
  ollama:
    embed-url: ${OLLAMA_EMBED_URL:http://127.0.0.1:11434/api/embeddings}
    embed-model: ${OLLAMA_EMBED_MODEL:nomic-embed-text}

# 개발 전용 시드 엔드포인트 (.env 에 GAK_DEV_SEED_ENABLED=true 추가 시 활성화)
gak:
  dev-seed:
    enabled: ${GAK_DEV_SEED_ENABLED:false}
  owner-token-secret: ${GAK_OWNER_TOKEN_SECRET:dev-gak-token-secret}
  internal-api-secret: ${GAK_INTERNAL_API_SECRET:dev-gak-internal-secret}
  cors:
    allowed-origins: ${GAK_CORS_ALLOWED_ORIGINS:http://localhost:3000}
  v2:
    spike:
      positive-threshold: 0.55
      negative-threshold: 0.45
    similarity:
      threshold: 0.72
      alert-cooldown-minutes: 3
  rag:
    ratio-a: 0.6
    ratio-b: 0.2

# Chzzk Open API 설정 (환경변수로 주입)
chzzk:
  api:
    base-url: https://openapi.chzzk.naver.com
    client-id: ${CHZZK_CLIENT_ID}
    client-secret: ${CHZZK_CLIENT_SECRET}
