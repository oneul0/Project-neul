spring:
  application:
    name: core-api

  r2dbc:
    url: r2dbc:postgresql://localhost:5432/neul_db
    username: neul_user
    password: neul_password

  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8083
